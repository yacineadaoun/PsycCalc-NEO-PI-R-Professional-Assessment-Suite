-e .

# Streamlit Cloud runtime requirements
streamlit>=1.30
numpy>=1.24
pillow>=10.0
opencv-python-headless>=4.7

# Optional (used by some utilities / plotting)
matplotlib>=3.7

# Dev (not required on Streamlit, safe to keep)
pytest>=7.0
pandas>=2.0
reportlab>=4.0
